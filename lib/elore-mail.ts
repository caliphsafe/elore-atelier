import nodemailer from "nodemailer";export async function mail(to:string,subject:string,text:string,replyTo?:string){const user=process.env.SMTP_USER,pass=process.env.SMTP_APP_PASSWORD;if(!user||!pass)throw new Error("Mail not configured");const t=nodemailer.createTransport({host:"smtp.gmail.com",port:465,secure:true,auth:{user,pass}});await t.sendMail({from:`"ELÖRE ATELIER" <${user}>`,to,subject,text,replyTo})}