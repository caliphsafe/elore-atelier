import{NextResponse}from"next/server";import{confirm,remove,verify}from"@/lib/elore-calendar";import{mail}from"@/lib/elore-mail";export async function GET(r:Request){try{const d=verify(new URL(r.url).searchParams.get("token")||"");if(d.action==="confirm"){await confirm(d.id,d.email,d.summary);return new NextResponse("<h1>Booking confirmed.</h1><p>Google Calendar will send the customer invitation.</p>",{headers:{"Content-Type":"text/html"}})}await remove(d.id);await mail(d.email,"ELÖRE booking request update","The requested time could not be confirmed. Please choose another available time.");return new NextResponse("<h1>Booking declined.</h1>",{headers:{"Content-Type":"text/html"}})}catch{return new NextResponse("<h1>Invalid or expired booking link.</h1>",{status:400,headers:{"Content-Type":"text/html"}})}}