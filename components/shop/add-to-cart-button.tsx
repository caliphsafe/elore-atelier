"use client";

import { AddFeedbackButton } from "@/components/shop/add-feedback-button";

export function AddToCartButton({ handle }: { handle: string }) {
  return <AddFeedbackButton handle={handle} />;
}
